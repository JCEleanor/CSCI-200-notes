- file is a stream of bits
- `ifstream` and `ofstream` comes from `fstream` class
- `>>` in `cin >>` is an **Extraction Operator**
- `<<` in `cout <<` is an **Insertion Operator** (point to where the data is flowing)

### reading files boilerplate
