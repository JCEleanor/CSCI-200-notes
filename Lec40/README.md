## Stack (LIFO)

can only push from the back and pop from the back

```c++
#include <stack>
```

`top` and `pop` to add and remove

### Depth First Search

## Queue (FIFO, just like pipes)

can only push to the back, but pop from the top

```c++
#include <queue>
```

`front` and `pop` to add and remove

### Breadth First Search

## Deque (not dicussed for this course)
